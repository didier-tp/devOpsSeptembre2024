pipeline {
    agent {
        docker {
		 image 'node:24' 
		}
    }
    stages {
     stage('from_git') { 
       steps { git url : 'https://github.com/didier-tp/vanilla_cypress_frontend' , branch : 'main' 
     } 
    }
    stage('npm_install') { 
      steps {
       sh 'npm install -g jest' 
     } 
    }
      stage('Test') {
            steps {
                sh 'npm run test'
            }
        }
    }
}